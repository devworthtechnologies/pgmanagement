// Replaced by CreatePropertyScreen.js when the app was wired to the real
// backend (2026-07-17) — the old local-only pgName/ownerName onboarding
// flow no longer applies now that properties are created via the API.
// File deletion isn't available in this environment, so this is left as an
// inert stub instead of a working screen. Do not import from here.
//
// See: src/screens/CreatePropertyScreen.js
export default null;
